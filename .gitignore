# Rust build artifacts
/target/

# Cargo.lock is committed: this workspace produces binaries, so a locked,
# reproducible dependency set is wanted. (Remove this note if it ever becomes
# a library-only workspace.)

# Model artifacts produced by training runs
*.safetensors
*.ckpt
/checkpoints/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
